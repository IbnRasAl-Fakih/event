# Event

## 🚀 Запуск проекта

### 📋 Требования

Для запуска проекта на локальной машине необходимо, чтобы было установлено следующее:

- **Java 22**  
  Проект разрабатывался и проверялся с использованием Java версии 22.
- **Apache Maven**
- **PostgreSQL**
  - Установленная и запущенная база данных PostgreSQL
  - Создана пустая база данных с именем **`event`**

Проверить установленные версии можно командами:

~~~bash
java -version
mvn -version
~~~

---

### ▶️ Шаги для запуска

1. Откройте терминал  
2. Перейдите в директорию проекта:
   ~~~bash
   cd /path/event
   ~~~
3. Запустите Spring Boot приложение:
   ~~~bash
   mvn spring-boot:run
   ~~~
   Чтобы не спаршивал одобрение когда завершается:
   ~~~bash
   mvn spring-boot:run < nul
   ~~~
